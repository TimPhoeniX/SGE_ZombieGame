# Zombie Game
Simple Game using Steering Behaviours techniques made with [SGE](https://github.com/xcodeassociated/SGE).

Walk using `WSAD` and control camera zoom with `QE`. Click `LMB` to fire.

Kill all zombies. Avoid getting swarmed as it will ultimately result in untimely Game Over.
